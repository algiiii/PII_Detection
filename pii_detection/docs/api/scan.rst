``scan`` — document PII scan
============================

Product CLI that composes extraction (B3) and detection (B4): given a path, it
extracts the document text and prints the PII found.

.. automodule:: pii_detection.scan
   :members:
   :member-order: bysource
